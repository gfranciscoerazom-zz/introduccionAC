#include <stdio.h>

// Variable declaration
extern int a, b, c;
float f, g, h;

int main(){
    // Variable definition.
    int a, b, c;
    // Variable initialization.
    a = 1;
    b = 34;
    c = a + b;

    printf("Value of sume is: %d", c);
    return 0;
}