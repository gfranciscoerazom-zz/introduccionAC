#include <stdio.h>

// Variable declaration
int a, b, c;
float f, g, h;

int main(){
    // Variable initialization.
    a = 1;
    b = 34;
    c = a + b;

    printf("Value of sume is: %d", c);
    return 0;
}